import * as styles from '../utils/tailwind.styles';

const Footer = () => {
    const footerStyles = {
        backgroundColor: `${styles.colors.gray2}`,
    }

    return <>
        <div style={footerStyles} className={`h-[400px]`}>

        </div>
    </>;
}

export default Footer;